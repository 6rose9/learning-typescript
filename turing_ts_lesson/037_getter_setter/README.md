# Calculator

Run the TypeScript calculator project.

Build and run (recommended):

```bash
npm install
npm start
```

Run without building (uses npx `ts-node`):

```bash
npm run dev
```

Quick test (non-interactive):

```bash
printf '5\n+\n3\nok\n' | node dist/app.js
```
