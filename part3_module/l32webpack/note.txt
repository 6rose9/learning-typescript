Recommand Typescript Config

https://github.com/tsconfig/bases

npm install --save-dev @tsconfig/recommended

"extends": "@tsconfig/recommended/tsconfig.json"

---------------------------------------------------------------------

Webpack (buddle your asset, scripts, images)

### install cli

https://webpack.js.org/guides/getting-started/#basic-setup

npm install webpack webpack-cli --save-dev

------------------------

### install server

https://webpack.js.org/guides/development/#using-webpack-dev-server

npm install --save-dev webpack-dev-server

------------------------

### install typescript

https://webpack.js.org/guides/typescript/

npm install --save-dev typescript ts-loader

------------------------

### create webpack config.js

https://webpack.js.org/guides/typescript/

import path from "node:path";
import { fileURLToPath } from "url";
import webpack from "webpack";

// in case you run into any TypeScript error when configuring `devServer`
import "webpack-dev-server";

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const config: webpack.Configuration = {
  entry: "./src/index.ts",
  mode: "development", add this line from https://webpack.js.org/configuration/mode/
  module: {
    rules: [
      {
        test: /\.tsx?$/,
        use: "ts-loader",
        exclude: /node_modules/,
      },
    ],
  },
  resolve: {
    extensions: [".tsx", ".ts", ".js"],
  },
  output: {
    filename: "bundle.js",
    path: path.resolve(__dirname, "dist"),
  },

  ### add devserver config to webpack.config.js (https://webpack.js.org/configuration/dev-server/)

  devServer: {
    static: {
      directory: path.join(__dirname, "public"),
    },
    compress: true,
    port: 9000,
  },
};

export default config;

------------------------

### config package.json

"watch" : " tsc -w"
"webpack" : "webpack" 
or 

"dev" : "webpack serve"

### create public folder

### create file public > index.html

### <script src="./bundle.js"> </script>